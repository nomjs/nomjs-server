version: '2'

services:
  redis:
    image: redis
    expose:
      - 6379

  nomjs-registry:
    build: .
    environment:
      NODE_ENV: development
    expose:
      - 9080
    volumes:
      - ${PWD}/:/nomjs-registry
    links:
      - redis
